import test from 'node:test';
import assert from 'node:assert/strict';
import {mkdtempSync,writeFileSync,mkdirSync,rmSync,chmodSync,readFileSync} from 'node:fs';
import {tmpdir} from 'node:os';
import {join} from 'node:path';
import {execFileSync} from 'node:child_process';
import {defaultBranch,normalizeSlug,branchPlan,commitPlanMessage,workingTreeEvidence,commitApprovedTask,scopedContentFingerprint,fingerprint,firstFreeBranch,createDeliveryBranch,branchState,assertApprovedPaths,clearApprovedStagedPaths} from '../extensions/delivery/io.mjs';
function repo(t){const d=mkdtempSync(join(tmpdir(),'delivery-git-'));t.after(()=>rmSync(d,{recursive:true,force:true}));execFileSync('git',['init','-q','-b','main',d]);writeFileSync(join(d,'a'),'base\n');execFileSync('git',['-C',d,'add','a']);execFileSync('git',['-C',d,'-c','user.name=T','-c','user.email=t@x','commit','-qm','base']);return d;}
test('detects default branch using local fallback',t=>{const d=repo(t);assert.equal(defaultBranch(d),'main');});
test('normalizes safe branch slugs and maps change types',()=>{assert.deepEqual(branchPlan('Add New API!!!','feature'),{prefix:'feature/',slug:'add-new-api',branch:'feature/add-new-api'});assert.equal(commitPlanMessage('bug','Fix issue'),'bug: Fix issue');assert.throws(()=>normalizeSlug('!!!'),/empty/i);});
test('selects the first free numeric branch suffix',t=>{const d=repo(t);for(const name of ['feature/topic','feature/topic-2'])execFileSync('git',['-C',d,'branch',name]);assert.equal(firstFreeBranch(d,{prefix:'feature/',slug:'topic'}),'feature/topic-3');});
test('creates the bound branch only from the clean expected base and detects drift',t=>{const d=repo(t);const base=branchState(d);const created=createDeliveryBranch(d,'feature/new-name',base.head);assert.equal(created.branch,'feature/new-name');assert.throws(()=>createDeliveryBranch(d,'feature/another',base.head),/base branch|base HEAD|worktree changed/i);});
test('working tree evidence is bounded and gives local recovery guidance',t=>{const d=repo(t);writeFileSync(join(d,'a'),'x'.repeat(50000));const evidence=workingTreeEvidence(d,['a']);assert.ok(evidence.length<=40000+300);assert.match(evidence,/repository-local/i);});
test('exact staging treats pathspec magic as a literal filename',t=>{const d=repo(t);const file='literal*[x]?.txt';writeFileSync(join(d,file),'changed\n');const record=commitApprovedTask(d,{changeType:'feature',title:'Magic path',files:[file],expectedHead:execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim(),snapshot:scopedContentFingerprint(d,[file])});assert.equal(record.paths[0],file);assert.equal(execFileSync('git',['-C',d,'show','--format=','--name-only',record.hash],{encoding:'utf8'}).trim(),file);});
test('staging rejects pre-staged and outside paths but supports deletion and untracked files',t=>{const d=repo(t);writeFileSync(join(d,'outside'),'x\n');execFileSync('git',['-C',d,'add','outside']);assert.throws(()=>assertApprovedPaths(d,['a']),/Pre-staged/);execFileSync('git',['-C',d,'restore','--staged','outside']);writeFileSync(join(d,'outside'),'x\n');assert.throws(()=>assertApprovedPaths(d,['a']),/outside.*approved/);execFileSync('git',['-C',d,'clean','-qf']);rmSync(join(d,'a'));const deleted=commitApprovedTask(d,{changeType:'bug',title:'Delete path',files:['a'],expectedHead:execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim(),snapshot:scopedContentFingerprint(d,['a'])});assert.equal(deleted.paths[0],'a');const file='new*[x]';writeFileSync(join(d,file),'new\n');const next=commitApprovedTask(d,{changeType:'feature',title:'Untracked path',files:[file],expectedHead:deleted.hash,snapshot:scopedContentFingerprint(d,[file])});assert.equal(next.paths[0],file);});
test('commit staging rejects a deterministic approved-file race before creating a commit',t=>{const d=repo(t);writeFileSync(join(d,'a'),'approved\n');const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();const accepted=scopedContentFingerprint(d,['a']);assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Race path',files:['a'],expectedHead:base,snapshot:accepted,onBeforeStage:()=>writeFileSync(join(d,'a'),'raced\n')}),/snapshot changed|authorization was invalidated/i);assert.equal(execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim(),base);assert.equal(execFileSync('git',['-C',d,'diff','--cached','--name-only'],{encoding:'utf8'}).trim(),'');});
test('prepared commit authorization hashes staged content',t=>{const d=repo(t);writeFileSync(join(d,'a'),'changed\n');const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();let prepared;const record=commitApprovedTask(d,{changeType:'feature',title:'Hash path',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a']),onPrepared:auth=>{prepared=auth;}});assert.equal(record.acceptedSnapshot,prepared.snapshot);assert.equal(record.acceptedDiffHash,prepared.diffHash);assert.equal(typeof prepared.diffHash,'string');assert.equal(prepared.snapshot.length,64);});
test('hook mutation invalidates commit resume authority',t=>{const d=repo(t);writeFileSync(join(d,'a'),'changed\n');const hook=join(d,'.git','hooks','pre-commit');writeFileSync(hook,'#!/bin/sh\nprintf "hook mutation\\n" > a\nexit 1\n');chmodSync(hook,0o755);const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();let failure;assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Hook path',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a'])}),error=>{failure=error;return Boolean(error.commitAuthorization);});assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Hook path',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a']),allowStaged:true,expectedAuthorization:failure.commitAuthorization}),/resume authority was invalidated/i);});
test('failed-hook invalidation clears only extension-owned staged paths',t=>{const d=repo(t);writeFileSync(join(d,'a'),'changed\n');const hook=join(d,'.git','hooks','pre-commit');writeFileSync(hook,'#!/bin/sh\nprintf "hook mutation\\n" > a\nexit 1\n');chmodSync(hook,0o755);const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();let failure;assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Clear path',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a'])}),error=>{failure=error;return Boolean(error.commitAuthorization);});rmSync(hook);assert.deepEqual(clearApprovedStagedPaths(d,['a']),['a']);assert.equal(execFileSync('git',['-C',d,'diff','--cached','--name-only'],{encoding:'utf8'}).trim(),'');assert.equal(readFileSync(join(d,'a'),'utf8'),'hook mutation\n');});
test('commit failure resumes with the unchanged accepted authorization',t=>{const d=repo(t);writeFileSync(join(d,'a'),'changed\n');const hook=join(d,'.git','hooks','pre-commit');writeFileSync(hook,'#!/bin/sh\nexit 1\n');chmodSync(hook,0o755);const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();let failure;assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Resume path',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a'])}),error=>{failure=error;return Boolean(error.commitAuthorization);});rmSync(hook);const record=commitApprovedTask(d,{changeType:'feature',title:'Resume path',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a']),allowStaged:true,expectedAuthorization:failure.commitAuthorization});assert.equal(record.baseHead,base);assert.equal(record.acceptedPaths[0],'a');});
test('successful hook mutation is never adopted as a clean delivery commit',t=>{const d=repo(t);writeFileSync(join(d,'a'),'changed\n');const hook=join(d,'.git','hooks','pre-commit');writeFileSync(hook,'#!/bin/sh\nprintf "pre-hook mutation\\n" > a\ngit add -- a\n');chmodSync(hook,0o755);const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Pre hook',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a'])}),error=>error.commitCompleted===true);assert.notEqual(execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim(),base);});
test('post-commit hook mutations leave an unadopted dirty result',t=>{const d=repo(t);writeFileSync(join(d,'a'),'changed\n');const hook=join(d,'.git','hooks','post-commit');writeFileSync(hook,'#!/bin/sh\nprintf "post-hook mutation\\n" > a\n');chmodSync(hook,0o755);const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Post hook',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a'])}),error=>error.commitCompleted===true);});
test('successful hook staging an outside path is rejected before adoption',t=>{const d=repo(t);writeFileSync(join(d,'outside'),'baseline\n');execFileSync('git',['-C',d,'add','outside']);execFileSync('git',['-C',d,'-c','user.name=T','-c','user.email=t@x','commit','-qm','outside']);writeFileSync(join(d,'a'),'changed\n');const hook=join(d,'.git','hooks','pre-commit');writeFileSync(hook,'#!/bin/sh\nprintf "hook outside\\n" > outside\ngit add -- outside\n');chmodSync(hook,0o755);const base=execFileSync('git',['-C',d,'rev-parse','HEAD'],{encoding:'utf8'}).trim();assert.throws(()=>commitApprovedTask(d,{changeType:'feature',title:'Outside hook',files:['a'],expectedHead:base,snapshot:scopedContentFingerprint(d,['a'])}),error=>error.commitCompleted===true&&/path set/.test(error.message));});
test('public schema leaves changeType optional for review plans',()=>{const source=readFileSync('extensions/delivery/index.ts','utf8');assert.match(source,/changeType:\s*Type\.Optional/);});
test('existing non-default branch can continue without creating another branch',t=>{const d=repo(t);execFileSync('git',['-C',d,'switch','-q','-c','feature/existing']);assert.equal(branchState(d).branch,'feature/existing');writeFileSync(join(d,'a'),'branch change\n');const record=commitApprovedTask(d,{changeType:'feature',title:'Existing branch',files:['a'],expectedHead:branchState(d).head,snapshot:scopedContentFingerprint(d,['a'])});assert.equal(record.branch,'feature/existing');});
